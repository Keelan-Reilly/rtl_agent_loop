[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "rtl_agent_loop"
version = "0.1.0"
description = "Deterministic FPGA DSE orchestration on top of CNN_FPGA"
requires-python = ">=3.12"

[tool.setuptools]
packages = ["rtl_agent_loop"]
